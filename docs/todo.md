# PHP Stubs Tasks

Build a typed PHP stub registry with tag-based YAML format, sealed class hierarchy, and dataflow summaries.

---

## Phase 1: Type System [DONE]

[x] Define PhpType enum (STRING, INT, FLOAT, BOOL, ARRAY, OBJECT, MIXED, VOID, NULL, CALLABLE, RESOURCE)
[x] Define Visibility enum (PUBLIC, PROTECTED, PRIVATE)
[x] Define StubParam data class (name, type, optional, defaultValue)
[x] Define StubRecord sealed class with 6 subtypes (Function, Method, PhpClass, Constant, ClassConstant, Property)

---

## Phase 2: YAML Format + Parser [DONE]

[x] Implement YamlStubParser with unified parse method
  Acceptance: `parse(reader, extension)` handles all 6 tags. Returns `ParseResult` with typed lists.
  - [x] Parse `function` tag: name, params, return, flowsToReturn
  - [x] Parse `method` tag: class, name, params, return, static, visibility, flowsToReturn
  - [x] Parse `class` tag: name, parent, interfaces, abstract, final
  - [x] Parse `constant` tag: name, type, value
  - [x] Parse `class_constant` tag: class, name, type, value, visibility
  - [x] Parse `property` tag: class, name, type, static, visibility
  - [x] `mapPhpType()` and `mapVisibility()` helper methods
  - [x] Reject unknown tags with StubIndexInvalidException
  - [x] Reject entries missing required fields per tag
  - [x] `./gradlew build` -- compiles clean
  - [x] `./gradlew test` -- all pass

[x] Implement StubLoader with index.txt discovery
  Depends on: YamlStubParser
  Acceptance: `loadAll()` reads `index.txt` manifest, parses all listed YAML files, builds StubRegistry with 6 typed maps.
  - [x] Read `index.txt` (auto-generated by Gradle processResources) for YAML file list
  - [x] Derive extension name from filename with `_\d+$` regex stripped
  - [x] Parse each YAML file via YamlStubParser
  - [x] Build registry maps keyed by normalized name
  - [x] Freeze all maps as unmodifiable
  - [x] `./gradlew build` -- compiles clean
  - [x] `./gradlew test` -- all pass

[x] Implement StubRegistry with 6 typed maps
  Acceptance: `StubRegistry` holds `Map<String, StubRecord.Function>`, `Map<String, StubRecord.PhpClass>`, `Map<String, StubRecord.Method>`, `Map<String, StubRecord.Constant>`, `Map<String, StubRecord.ClassConstant>`, `Map<String, StubRecord.Property>`.
  - [x] `./gradlew build` -- compiles clean
  - [x] `./gradlew test` -- all pass

[x] Implement PhpStubs facade with suffix index
  Acceptance: `PhpStubs` provides existence checks, typed record retrieval, bulk name access, keyword/scalar synthetic records, and O(1) suffix index for method/class-constant lookup.
  - [x] `containsFunc/Class/Method/Const` existence checks
  - [x] `searchFunc` returns `StubRecord.Function?`
  - [x] `searchClass` returns `StubRecord.PhpClass?`
  - [x] `searchMethod` returns `Pair<String, StubRecord.Method>?`
  - [x] `searchGlobalConst` returns `StubRecord.Constant?`
  - [x] `searchClassConst` returns `StubRecord.ClassConstant?`
  - [x] `getAllFuncNames/ClassNames/MethodNames/ConstNames` bulk accessors
  - [x] `getKeywordFuncNames` and `getScalarTypeNames`
  - [x] `normalize()` strips leading `/` and `\`, lowercases
  - [x] Suffix index via `buildSuffixIndex()` for O(1) method/class-constant lookup
  - [x] `./gradlew build` -- compiles clean
  - [x] `./gradlew test` -- all pass

Test coverage: 106 unit tests + 16 performance tests (9 test classes).

---

## Phase 3: Data Extraction

Write extraction script to generate YAML files from upstream sources.

[ ] Write extraction script
  Acceptance: Script reads phpstorm-stubs + Psalm stubs, outputs per-extension YAML files in category subdirectories.
  - [ ] Parse phpstorm-stubs with nikic/php-parser: extract functions, methods, classes, constants, class_constants, properties
  - [ ] Parse Psalm stubs `@psalm-flow` annotations: extract flowsToReturn mappings
  - [ ] Apply heuristic flowsToReturn for functions without @psalm-flow
  - [ ] Merge signatures + dataflow into per-extension YAML
  - [ ] Output to `stubs/**/*.yaml`

[ ] Generate YAML files from upstream
  Depends on: extraction script
  Acceptance: Complete YAML files covering all PHP extensions from phpstorm-stubs. flowsToReturn populated from Psalm where available.
  - [ ] Run extraction script against latest phpstorm-stubs + Psalm stubs
  - [ ] Verify output covers ~5000 functions, ~500 classes, ~300 constants
  - [ ] Spot-check flowsToReturn accuracy for 20 well-known functions
  - [ ] Commit generated YAML files

---

## Phase 4: Cleanup [DONE]

[x] Delete old files and types
  Acceptance: Only YAML stub files, sealed StubRecord hierarchy, and SnakeYAML dependency remain. Zero unused types or imports.
  - [x] `./gradlew build` -- compiles clean
  - [x] `./gradlew test` -- all pass

---

## Phase 5: cobraphp-core Integration

[ ] Update cobraphp-core consumers
  Depends on: Phase 2 complete
  Acceptance: cobraphp-core uses phpstubs for signatures + dataflow, owns taint rules separately.
  - [ ] Migrate BuiltinExt.kt to use StubRecord sealed class subtypes
  - [ ] Load taint rules from cobraphp-core's own YAML files
  - [ ] `./gradlew build` -- compiles clean (cobraphp-core)
  - [ ] `./gradlew test` -- all pass (cobraphp-core)

---

## Data Sources Reference

| Data | Source | Extracted Field |
|------|--------|----------------|
| Function signatures | JetBrains/phpstorm-stubs | tag, name, params, return |
| Class hierarchies | JetBrains/phpstorm-stubs | tag, name, parent, interfaces |
| Constants with values | JetBrains/phpstorm-stubs | tag, name, type, value |
| Class constants | JetBrains/phpstorm-stubs | tag, class, name, type, value |
| Properties | JetBrains/phpstorm-stubs | tag, class, name, type, visibility |
| Methods | JetBrains/phpstorm-stubs | tag, class, name, params, return |
| flowsToReturn | vimeo/psalm `@psalm-flow` | flowsToReturn indices |
| flowsToReturn (fallback) | Heuristic rules | flowsToReturn candidates |
